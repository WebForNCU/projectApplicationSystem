<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.security.dao.BatchDao">
    <resultMap id="result" type="com.spring.security.entity.Batch">
        <result column="id" property="id" jdbcType="INTEGER" />
        <result column="batch_name" property="batch_name" jdbcType="VARCHAR" />
        <result column="build_date" property="build_date" jdbcType="TIMESTAMP" />
        <result column="item_name" property="item_name" jdbcType="VARCHAR" />
    </resultMap>
    <select id="queryBatchList" resultMap="result">
        select * from batch;
    </select>

    <select id="queryBatchByBatchName" parameterType="String" resultMap="result">
        select * from batch where batch_name = #{batch_name}
    </select>
</mapper>