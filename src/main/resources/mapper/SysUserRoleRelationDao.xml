<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.security.dao.SysUserRoleRelationDao">

    <resultMap type="com.spring.security.entity.SysUserRoleRelation" id="SysUserRoleRelationMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--根据userId查询roleId-->
    <select id="queryRoleIdByUserId" resultMap="SysUserRoleRelationMap">
        select
            id, role_id, role_id
        from sys_user_role_relation
        where user_id = #{userId}
    </select>

    <!--插入-->
    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sys_user_role_relation(user_id, role_id)
        values (#{userId}, #{roleId})
    </insert>
</mapper>